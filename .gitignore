# Build output
build/
# GBDK tarball (toolchain is vendored extracted)
/tools/gbdk.tar.gz
tools/gbdk.tar.gz
# Vendored toolchain dir (local build tooling, re-download on fresh clone)
tools/gbdk/
# Stray link outputs at root
*.ihx
*.gb
# Temp
/tmp/
.DS_Store
